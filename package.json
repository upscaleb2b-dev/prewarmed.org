{
  "name": "prewarmed-org",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/check-pricing.mjs && astro build",
    "build:live": "node scripts/check-pricing.mjs --live && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.1.1",
    "@astrojs/sitemap": "^3.2.1"
  }
}
